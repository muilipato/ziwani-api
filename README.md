# Church Website Gallery API

A lightweight Ruby (Sinatra) REST API that exposes images stored in an Amazon S3 bucket for the AIC Ziwani Church website.

The API is designed to work alongside a React frontend by listing available gallery albums and retrieving the images contained within each album.
